import Head from "next/head";import type {GetServerSideProps} from "next";
type Proposal={proposalCode:string;status:string;total:number;currencyCode:string;updatedAt:string;draftCode:string;draftVersion:number;customerName:string|null;siteAddress:string|null};type Props={locked?:boolean;proposals?:Proposal[]};const money=(n:number)=>new Intl.NumberFormat("en-AU",{style:"currency",currency:"AUD",maximumFractionDigits:0}).format(n);
export default function Proposals({locked,proposals=[]}:Props){if(locked)return <main className="sales-locked"><div><p>BETTER HOME</p><h1>Sales Studio</h1><span>Internal access is not configured on this environment.</span><a href="/sales">Return</a></div></main>;return <><Head><title>Proposals — Sales Studio</title><meta name="robots" content="noindex,nofollow"/></Head><main className="sales-detail"><nav><a href="/sales">← Sales Studio</a><span>BETTER HOME</span></nav><header><div><p>PROPOSALS</p><h1>Fixed decisions.</h1><span>Each Proposal belongs to one customer and one immutable Draft version.</span></div></header><section className="sales-list"><div className="sales-list-head"><div><p>COMMERCIAL REVIEW</p><h2>Proposals</h2></div></div>{proposals.length?<div className="sales-table">{proposals.map(x=><article key={x.proposalCode}><div><b>{x.customerName||"Unnamed customer"}</b><span>{x.siteAddress||x.draftCode}</span></div><div><small>{x.proposalCode}</small><span>Draft v{x.draftVersion}</span></div><div><small>STATUS</small><span>{x.status.replaceAll("_"," ")}</span></div><strong>{money(x.total)}</strong><a className="sales-open" href={`/sales/proposals/${x.proposalCode}`}>Open</a></article>)}</div>:<div className="sales-empty"><h3>No Proposals yet.</h3><p>A Proposal appears here after a reviewed Draft is converted.</p><a href="/sales">Return to Drafts</a></div>}</section></main></>}
export const getServerSideProps:GetServerSideProps<Props>=async context=>{const {actorFromRequest}=await import("@/server/sales-auth");const actor=actorFromRequest(context.req);if(!actor)return {redirect:{destination:"/login?next=%2Fsales%2Fproposals",permanent:false}};const {readContext,salesStudioService}=require("@bht/product-os/v2");const os=readContext.createProductOsV2ReadContext();try{return {props:{proposals:JSON.parse(JSON.stringify(await salesStudioService.createSalesStudioService(os.prisma).proposals(actor)))}};}finally{await os.disconnect();}};
