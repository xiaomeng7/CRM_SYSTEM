/**
 * @bht/shared
 * Shared constants, utilities, and (future) types/schemas.
 * Keep domain-agnostic; no CRM or ServiceM8 business logic.
 */

module.exports = {
  // Reserved for future: constants, validation schemas, shared utils
};
