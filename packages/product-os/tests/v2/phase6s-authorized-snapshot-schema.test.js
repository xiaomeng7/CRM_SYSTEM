const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");const sql=fs.readFileSync(path.join(__dirname,"../../prisma/migrations/20260721120000_add_handoff_authorized_snapshot/migration.sql"),"utf8");test("authorized handoff stores the reviewed payload snapshot",()=>{assert.match(sql,/authorized_payload_snapshot/);assert.match(sql,/authorized_snapshot_chk/);assert.doesNotMatch(sql,/\bDROP\b|\bDELETE\s+FROM\b|\bTRUNCATE\b/i);});
