# Dependencies
node_modules/
apps/crm/node_modules

# Env (never commit secrets)
.env
.env.*
!.env.example

# Lockfile: monorepo uses pnpm; ignore npm lock to avoid confusion
package-lock.json

# Claude Code internal
.claude/

# Build / runtime
*.log
.DS_Store
dist/
build/
.vercel
.netlify
